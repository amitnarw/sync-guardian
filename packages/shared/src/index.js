/**
 * @sync-guardian/shared
 * 
 * Shared types, constants, and utilities used across all SyncGuardian apps.
 */

// Types
const { DeviceStatus } = require('./constants');

module.exports = {
  DeviceStatus,
};
